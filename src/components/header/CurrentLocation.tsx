import React from 'react';
import { ICurrentLocation } from '../../ts/type-defs/header'

const CurrentLocation:React.FC<ICurrentLocation> = ({zipCode,setZipCode}) => {
  return (
    <div>CurrentLocation</div>
  )
}

export default CurrentLocation